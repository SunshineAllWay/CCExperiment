from connection import STSConnection from boto . regioninfo import RegionInfo def regions ( ) : return [ RegionInfo ( name = 'us-east-1' , endpoint = 'sts.amazonaws.com' , connection_cls = STSConnection ) ] def connect_to_region ( region_name , ** kw_params ) : for region in regions ( ) : if region . name == region_name : return region . connect ( ** kw_params ) return None def get_region ( region_name , ** kw_params ) : for region in regions ( ** kw_params ) : if region . name == region_name : return region return None
