""" This module provides an interface to the Elastic Compute Cloud (EC2) service from AWS. """ from boto . ec2 . connection import EC2Connection def regions ( ** kw_params ) : c = EC2Connection ( ** kw_params ) return c . get_all_regions ( ) def connect_to_region ( region_name , ** kw_params ) : for region in regions ( ** kw_params ) : if region . name == region_name : return region . connect ( ** kw_params ) return None def get_region ( region_name , ** kw_params ) : for region in regions ( ** kw_params ) : if region . name == region_name : return region return None
