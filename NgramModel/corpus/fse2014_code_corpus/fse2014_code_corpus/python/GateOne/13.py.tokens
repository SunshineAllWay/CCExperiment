__author__ = 'Dan McDougall <daniel.mcdougall@liftoffsoftware.com>' import os , sys , unittest , time from pprint import pprint cwd = os . getcwd ( ) terminal_dir = os . path . abspath ( os . path . join ( cwd , '../' ) ) sys . path . append ( terminal_dir ) import terminal ROWS = 56 COLS = 210 class Test1Coding ( unittest . TestCase ) : def test_1_parsing_performance ( self ) : term = terminal . Terminal ( ROWS , COLS ) start = time . time ( ) for i , x in enumerate ( xrange ( 4 ) ) : with open ( 'saved_stream.txt' ) as stream : for char in stream . read ( ) : term . write ( char ) print ( i ) end = time . time ( ) elapsed = end - start print ( 'It took %0.2fms to process the input' % ( elapsed * 1000.0 ) ) pprint ( term . dump_html ( ) ) if __name__ == "__main__" : print "Date & Time:\t\t\t%s" % time . ctime ( )
